commands used to std input
>>linux cmds which uses only std inputs
>>linux cmds which uses only std  outputs
>>linux cmds which uses both std input and outputs


Standard Inputs and outputs

wc Commands

pipe symbol

cut cmd

grep cmd

